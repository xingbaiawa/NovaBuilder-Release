## NovaBuilder Binary Repository

This is a repository that hosts the binary files for **NovaBuilder**.

Note that you need to use an **API Token** to connect to the authentication server.

If you need to use a custom authentication server address, please use the command line argument `-A`.
